Rewrite screenshot baseline images will live under this directory once the legacy capture workflow lands.

Only the scaffold files in this directory are tracked. Generated baseline images and approval artifacts stay ignored until `RW-TEST-006` populates them.
