apply from: "${rootDir}/src/RewriteBuildLogic/gradle/rewrite-library.gradle"

description = 'Normalized client-side stores, selectors, and controller-facing model helpers for the rewrite client.'

dependencies {
    api project(':RewriteProtocol')
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:1.10.2"
}
